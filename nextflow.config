nextflow.enable.moduleBinaries = true    
nextflow.enable.dsl=2

process {
    withLabel: big_mem {
        maxForks = 1
        cpus = 6
        memory = 8.GB
    }
}

profiles {
    conda {
        conda.enabled = true 
        conda.useMamba = true 
    }
}

params {

    help = false
    publish_dir = "/home/nicolai/Documents/Arbeit/InterNet_Xplorer/src/grn-nextflow/tmp_results"
    grn_mode = false
    create_metacells = true    

    n_runs = 10
    boostdiff {
        n_estimators = 100
        n_features = 1500
        n_subsamples = 30
        n_processes = 8

        top_n_targets = 20
        top_n_edges = 100
    }
    grnboost2 {
        top_n_edges = 100
    }
    diffcoex {
        top_n_edges = 100
    }
    z_score {
        top_n_edges = 100
    }

}
